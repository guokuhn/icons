# Server Configuration
PORT=3000
NODE_ENV=development

# Storage Configuration
ICON_STORAGE_PATH=./data/icons

# Cache Configuration
CACHE_TTL=86400

# Icon Conflict Handling
# Strategy for handling icon name conflicts: 'overwrite' or 'reject'
# - overwrite: Replace existing icon and save version
# - reject: Return 409 Conflict error (default)
ICON_CONFLICT_STRATEGY=reject

# API Security
API_KEY=your-api-key-here

# CORS Configuration
# Controls which domains are allowed to make cross-origin requests to the API
# 
# Format options:
# 1. Single domain: CORS_ORIGIN=http://localhost:5173
# 2. Multiple domains (comma-separated): CORS_ORIGIN=http://localhost:5173,https://app.example.com,https://admin.example.com
# 3. Allow all domains (development only): CORS_ORIGIN=*
# 4. Reject all cross-origin requests (empty or not set): CORS_ORIGIN=
#
# Important notes:
# - Each domain must include the full protocol (http:// or https://)
# - Port numbers must match exactly (e.g., :3000 vs :5173 are different origins)
# - Subdomains are treated as separate origins (app.example.com ≠ admin.example.com)
# - For production, explicitly list allowed domains instead of using wildcard (*)
# - Same-origin requests (no Origin header) are always allowed
#
# Examples by environment:
# Development: CORS_ORIGIN=http://localhost:5173,http://localhost:3000
# Testing: CORS_ORIGIN=https://test.example.com,https://test-admin.example.com
# Production: CORS_ORIGIN=https://app.example.com,https://admin.example.com,https://www.example.com
CORS_ORIGIN=http://localhost:5173

# Figma Integration (Optional - Phase 3)
# FIGMA_API_TOKEN: Personal access token from Figma account settings
# Get it from: https://www.figma.com/developers/api#access-tokens
# FIGMA_FILE_ID: The file ID from your Figma file URL
# Example URL: https://www.figma.com/file/FILE_ID/File-Name
# Leave empty to disable Figma integration
FIGMA_API_TOKEN=
FIGMA_FILE_ID=

# Rate Limiting
RATE_LIMIT_READ=1000
RATE_LIMIT_WRITE=100
